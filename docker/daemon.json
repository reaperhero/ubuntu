# path = /etc/docker/daemon.json
# systemctl restart docker
# docker 代理加速地址

{
  "graph": "/data/docker",
  "insecure-registries": [
      "0.0.0.0/0"
  ],
  "registry-mirrors": [
      "http://hub-mirror.c.163.com"
  ]
}
